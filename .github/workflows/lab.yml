name: Lab
on: push
jobs:
    build-project:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v4
            
            - name: formater_lib
              run: cmake --build ${{ github.workspace }}/formatter_lib/

            - name: formater_ex_lib
              run: cmake --build ${{ github.workspace }}/formatter_ex_lib/

            - name: hello_world
              run: cmake --build ${{ github.workspace }}/hello_world_application/

            - name: solver_lib
              run: cmake --build ${{ github.workspace }}/solver_lib/

            - name: solver_application
              run: cmake --build ${{ github.workspace }}/solver_application/